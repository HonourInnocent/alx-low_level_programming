#include "main.h"
void print_alphabet(void)
int main()
{
    char c;
    for (c = 'a'; c <= 'z'; ++c)
        printf("%c ", c);
    return 0;
    void print_alphabet(void)
    {
	    //print all the letters of the alphabet in lowercase
    }
}

