#include "main.h"

/**
 * _memset - fills memory n with constant byte
 * @s: string
 * @b: byte
 * @n: number of memory
 *
 * Return: A string
 */

char *_memset(char *s, char b, unsigned int n)

{
	unsigned int s;

	for (a = 0; a  < n;)
		x++;
	{
		s[x] = b;
	
	}
	return (s);
}
