#include "main.h"

/**
 * _memset - fills memory n with constant byte
 * @s: string
 * @b: byte
 * @n: number of memory
 *
 * Return: 0 (success)
 */

char *_memset(char *s, char b, unsigned int n)
{
	unsigned int a;

	for (x = 0; x < n;)
		x++;
		s[x] = b;
	return (s);
}
